{
  "name": "plotter-mcp",
  "version": "1.0.0",
  "description": "MCP server for the polar plotter — lets Claude send drawing commands directly",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build:core": "node build-core.mjs",
    "prestart": "npm run build:core",
    "start": "node index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
