// Import for side effects: each module self-registers into the registry on load.
// Import this once (the Studio tab does) so listModules() sees them all.
import "./box";
import "./circle";
import "./square";
export {};
