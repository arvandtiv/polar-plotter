# Round 29 — Klee #15 "Growth and decrease" (REDO) — REJECTED / SKIPPED

## Outcome
**Skipped, not scored.** User (in chat): *"repeated work (another flow field) — do something new."*
R28 (a `growthField` comma/mark field) had been rejected "not worthy"; R29 redid the same concept
as a BREATHING flow field on the winning `flowWhirls` engine (R25/R27's swirling streamline currents,
length/density swelling to a crest band). Technically on-taste and well-executed — but the user's
objection was not the execution, it was **sameness**: three flow-field rounds in a row (R25, R27, R29).

## The lesson (already folded into LEARNINGS)
- **A winning engine goes stale fast.** flowWhirls won R27 and reliably reads as organic flow, so the
  instinct was to keep leaning on it. That is exactly the "genre rut" the user has now called out
  twice (the located-figures #237/#238/#274 streak, and now the flow streak). **Never run the same
  engine 3+ rounds.** Flow is not banned — it is *spent for now*.
- **The fix for "growth and decrease" was over-fit to the current winner.** Rendering every new Klee
  concept as "a modulation of the flow field" (the R28 lesson) collapses variety. The concept
  deserved its OWN visual language. Growth/decrease → literally *growth*: a dendritic branching
  structure (tree/coral/delta/veins) whose branch length decays each generation = growth-and-decrease
  made structural, not a density envelope on flow.

## What was built in response
A genuinely new generator, **`branching`** (`console/src/lib/modules/branching.ts`): organic
dendritic growth that fills the wall — irregular split counts, hand-drawn curved branches, shrinking
length per generation (the "decrease"), tropism pull toward a grow direction, roots along an edge or
radial from centre. New visual language, still through the organic/hand-drawn lens. Wired into the
registry; awaiting the user's next instruction pick before authoring a round with it.

## Standing rule reaffirmed
⛔ Do not pick the next instruction. Wait for the user.
