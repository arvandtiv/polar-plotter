# Round 21 reflection — LeWitt #915 (wobbly bands via `sheets`) — REJECTED, tool banned

**User ranking:** [] (empty — whole round rejected)
**User note (chat):** *"this round is not good and must be excluded all together, please avoid using
this tool."* (the tool = `sheets`)
**Scored:** 2026-07-01

## Headline — `sheets` is banned; "wobbly" ≠ fine-line fabric
The `sheets` generator (randomly displaced grid columns interpolated into flowing curtain lines) was
rejected outright, and the user asked to **avoid the tool entirely**. So even though I aimed for the
"wobbly feel" they asked for after R20, the *execution* — dense fields of fine near-parallel wavy
lines forming a woven fabric/moire texture — is NOT what "wobbly" means to them. The fine-line
fabric reads mechanical/noisy, and the dense ones tipped into mud (also a paper-rip risk).

## Re-reading "wobbly" (two misses in a row → recalibrate)
R20 (clean circles) and R21 (fine-line fabric) both missed. Triangulating what the user DOES like:
the hits were **round 11 (stroke-field FLOW)** and **round 14 (arc SCATTER)** — both are **sparser,
open, individual wandering marks with clear directional flow and breathing room**, not a dense
edge-to-edge texture. So "wobbly" = **loose, open, hand-wandering individual lines/strokes with
negative space** — NOT a wall-to-wall fine-line weave. Favor openness + a few expressive wandering
lines over dense mechanical fill.

## Transferable rules (→ LEARNINGS)
1. **BANNED tool: `sheets`** (displaced-column curtains) — like Truchet, don't use it.
2. **"Wobbly" means loose, open, individual wandering lines with directional flow + negative space**
   (round-11 flow, round-14 scatter), NOT a dense edge-to-edge fine-line fabric/weave. When going
   organic, keep it SPARSE and expressive, not a full mechanical texture.
3. Dense fine-line fields also violate the paper-rip limit and read as mud — a double reason to keep
   organic work open.
